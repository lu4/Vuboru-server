'use strict';

angular.module('vuboruServerApp')
  .factory('test', ['dataRequest', function (data) {
//function (collection) {
//    var array = collection.find().toArray();
//
//    for (var i = 0; i < array.length; i++) {
//        var item = array[i];
//
//        if (item.createdOn) {
//            item.authoredOn = item.createdOn;
//            item.authoredBy = item.createdBy;
//            delete item.createdOn;
//            delete item.createdBy;
//        }
//
//        collection.update({ _id: item._id }, item);
//    }
//}
    }]);
